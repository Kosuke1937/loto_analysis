name: Analyze Mini Loto Gap Patterns 1403
on:
  push:
    paths:
      - 'scripts/analyze_miniloto_gap_patterns_1403.py'
      - '.github/workflows/analyze-miniloto-gap-patterns-1403.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/analyze_miniloto_gap_patterns_1403.py
      - name: Commit result
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/miniloto-gap-patterns-1403.json
          git diff --cached --quiet || git commit -m 'Analyze Mini Loto adjacent gap patterns'
          git push
