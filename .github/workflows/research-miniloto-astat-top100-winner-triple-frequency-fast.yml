name: Research Mini Loto A-Stat Top100 Winner Triple Frequency Fast
on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/research_miniloto_astat_top100_winner_triple_frequency_fast.py'
      - '.github/workflows/research-miniloto-astat-top100-winner-triple-frequency-fast.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check numpy
      - run: python scripts/research_miniloto_astat_top100_winner_triple_frequency_fast.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '4189824+github-actions[bot]@users.noreply.github.com'
          git checkout -- data/miniloto-a-nearhit-ranks-800-1399.json 2>/dev/null || true
          git add data/miniloto-astat-top100-winner-triple-frequency-fast.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Update fast A-Stat winner triple frequency'
          git pull --rebase origin main
          git push origin HEAD:main
