name: Generate Mini Loto 1402 Fast Rebuild
on:
  push:
    paths:
      - 'scripts/generate_miniloto_1402_fast_rebuild.py'
      - '.github/workflows/generate-miniloto-1402-fast-rebuild.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --disable-pip-version-check numpy
      - run: python scripts/generate_miniloto_1402_fast_rebuild.py
      - name: Commit result
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/miniloto-1402-fast-rebuild.json
          git diff --cached --quiet || git commit -m 'Generate Mini Loto 1402 fast rebuild'
          git push