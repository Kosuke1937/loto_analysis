name: Update Mini Loto Direct Rank Summary
on:
  push:
    paths:
      - 'scripts/update_miniloto_direct_rank_summary.py'
      - '.github/workflows/update-miniloto-direct-rank-summary.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/update_miniloto_direct_rank_summary.py
      - name: Commit app update
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add miniloto-development.html
          git diff --cached --quiet || git commit -m 'Record Mini Loto direct ranking summary'
          git push
